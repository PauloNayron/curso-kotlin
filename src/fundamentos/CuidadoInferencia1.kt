package fundamentos

fun main(args: Array<String>) {
     /*
     o kotlin precisa que seja atribuido o tipo, ou que a variável seja inicializada
     de forma que a linha abaixo gera erro
     */
//    var a
    var a: Int
    var b = 2

    a = 10

    print(a+b)
}